/* const controlBaile = edad => {
    if ((edad > 18 && edad <= 40) || edad >= 55) {
        return "Puede pasar"
    } else if (edad === 18) {
        return "Tienes 18, puedes pasar"
    } else if (edad <= 0) {
        return "Disculpa, no podrias ser un ser humano"
    } else if (edad === 17 || edad === 16) {
        return "Puedes pasar con autorizacion de tus padres"
    } else {
        return "No, no puedes pasar, eres menor de edad"
    }
}



let edad = 16

switch (Number.isInteger(edad)) {
    case (edad > 18 && edad <= 40) || edad >= 55:
        console.log("Puede pasar")
        break;
    case edad === 18:
        console.log("Tienes 18, puedes pasar")
        break;
    case edad <= 0:
        console.log("Disculpa, no podrias ser un ser humano")
        break;
    case edad === 17 || edad === 16:
        console.log("Puedes pasar con autorizacion de tus padres")
        break;
    default:
        console.log("No, no puedes pasar, eres menor de edad")
        break;
} */






/* const edad = 20

//condicion ? En el caso de true : en el caso de false
edad >= 18 ? console.log("Tienes mas de 18") : console.log("Eres menor de 18");

if(edad >= 18) {
    console.log("Tienes mas de 18")
} else {
    console.log("Eres menor de 18")
}
*/


//                  0    ,  1   ,   2
//console.log(nombres[2])
//console.log(nombres.length)
//console.log(nombres[nombres.length - 1])
//nombres.push("Ada")
//nombres.pop()
//nombres.unshift("Angel")
//nombres.shift()
//console.log(nombres.join())
//console.log(nombres.indexOf("Car"))

/* if(nombres.includes("Car")) {
    console.log("Si si existe")
} else {
    console.log("No no existe")
} */


const nombres = ["Angel", "Carla", "Clau"]

/* for (inicio; condición ; modificador) {
    //código que se ejecutará en cada repetición
} */

/* for (let i = 0; i < nombres.length; i++) { 
    console.log(nombres[index])
} */

// En primer instancia (inicio) se guarda la variable index (Solo lo hace una vez al principio)
// Antes de ejecutar (condicion) el codigo entre {} evalua la condicion para ver si es true
// Al terminar la ejecucion del codigo entre {} ejecuta el modificador.



/* let index = 0;
while (index < nombres.length) {
    console.log(nombres[index])
    index++
}
*/


